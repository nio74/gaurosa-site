# Reindirizza tutto a index.php (tranne file esistenti)
RewriteEngine On
RewriteBase /

# Se il file esiste fisicamente, servilo
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Tutto il resto passa a index.php
RewriteRule ^ index.php [L]
